# PA4 题目汇总

课程：CST数据结构（2025秋）

原始课程页面：https://dsa.cs.tsinghua.edu.cn/oj/course.shtml?courseid=230

---

# CST X-2-1 Virus

---

### **题目描述**

有一个`n * m`的01矩阵，假设矩阵第`i`行第`j`列为 0，代表该处是一个病毒。

病毒会随着时间同时向多个方向扩散，假设`x`行`y`列有一个病毒，则`i`行`j`列的点会在`|x - i| + |y - j|`秒后被他感染，而一个点`(i, j)`被病毒感染所花的时间定义为第一次被某病毒感染所花的时间。

你只需统计所有非病毒点被病毒感染所花的时间之和即可。

### **输入**

第一行有两个正整数`n, m`，表示矩阵的长和宽。

接下来`n`行，每行一个长为`m`的字符串，保证字符串里只会出现 0 和 1。

保证所有字符串中存在 0。

### **输出**

你只用输出一个整数，表示所有非病毒点被病毒感染所花的时间之和。

### **输入样例**

```
3 4
0110
0100
0001
```

*此样例是第 1 个测试点。

### **输出样例**

```
4
```

### **样例解释**

`(1, 2)`的 1，会被`(1, 1)`的0感染，感染时间为`|1 - 1| + |2 - 1| = 1`。

`(1, 3)`的 1，会被`(1, 4)`或者`(2, 3)`的 0 感染，感染时间为`|1 - 1| + |3 - 4| = 1`或者`|1 - 2| + |3 - 3| = 1`

同理，`(2, 2)`的 1，会被`(2, 1)`或者`(3, 2)`或者`(2, 3)`的 0 感染，`(3, 4)`的 1 会被`(2, 4)`或者`(3, 3)`的 0 感染。

感染时间总和为 4。

### **数据范围**

1 <= n, m <= 1000

### **资源限制**

时间限制： 0.5 sec

空间限制： 256 MB

**本题允许使用STL**

### **提示**

● 图

● [ BFS ]

---

# CST X-2-2 Game

---

### **描述**

熊熊最近迷上了一款游戏，这款游戏有N个关卡，编号为1到N。熊熊一开始在1号关卡，而boss在N号关卡，通关boss关卡表示熊熊已经通关了整个游戏。

关卡和关卡之间有道路联通，任意两个关卡之间至多存在一条道路，熊熊每次能选择一个**和已通关关卡存在道路直接连接**的任意关卡进行挑战。如下图，熊熊已经通关了关卡1和关卡3，接下来可以从关卡2,关卡4和关卡5中任选一个关卡进行挑战。在最开始熊熊只能选择关卡1进行挑战。

![](../附件/d2d8a5a373724acaf8f4610003926f65014a64a0.png)

熊熊是一个资深游戏玩家，他计算出了自己通关每一个关卡的时间。熊熊想让你帮忙计算出，如果要通关游戏，最少需要多少时间，并且有多少种不同的通关顺序可以达成这个最少通关时间。

### **输入**

第一行输入两个整数N和M，分别表示关卡数量和道路数量。

接下来一行N个整数t[i]，表示熊熊计算出的通关每个关卡的时间。

接下来M行每行输入两个整数u和v，表示关卡u和关卡v之间存在一条道路。

保证输入数据一定存在至少一个通关顺序能够让熊熊通关游戏。

### **输出**

第一行输出熊熊通关游戏的最短时间。 第二行输出有多少种通关顺序能够以最短时间通关游戏。由于方案数可能很大，你只需要输出方案数对10^9+7的余数。

### **输入样例**

```
6 6
1 2 2 3 4 5
1 2
1 3
2 4
3 4
3 5
4 6
```

### **输出样例**

```
11
2
```

### **样例解释**

总共有2种通关顺序：1->2->4->6和1->3->4->6，这两种通关顺序的用时都是11。

### **限制**

1 <= N,M <= 100,000

1 <= t[i] <= 10,000

时间限制：1 sec

空间限制：256 MB

**本题允许使用STL**

---

# CST X-2-3 Petroleum

---

### **题目描述**

小明经营了一家石油公司，最近新勘探到一片油田需要开采。

石油开采时需要向下钻井，石油井必须保持竖直向下，否则会有塌方的危险。 石油井挖出石油以后必须停止向下挖，也就是说如果一块石油区域上方全被其他石油区域遮挡住，那么这块石油区域就无法开采。 由于开采能力有限，公司挖掘的石油井最多只能达到 hh 的深度。

油田中的石油分布不均，小明使用了一种最新的勘探仪器，能够获取每一片石油区域的具体位置。 仪器检测到的每一片石油区域都是一个矩形形状，不同的石油区域可能会有重叠。 多个重叠的石油区域是连通的，整体构成了一片大的石油区域，钻到其中任意一片就可以把区域中所有的石油都抽出来。

以下图为例，图中A块可被直接挖出。B块正上方完全被遮挡住，所以无法开采。C块未被遮挡，但是本身处于深度 hh 以下，所以无法开采。 DEF三块联通，且F块可以被开采，所以这三块组成的联通区域均可被开采。

![](../附件/5975ef6da3f138a38c0c4f526780fae3360a7f1d.png)

由于每天都能勘探到新的石油区域，小明希望请你帮忙参与制定公司的开采计划。

### **输入**

第一行为两个正整数，深度限制 hh 和一个正整数 NN，最小深度小于等于 hh 的石油区域才能被石油井挖到。 接下来的 NN 行，包含以下三种格式的事件：

1. A x1 y1 x2 y2A\ x1\ y1\ x2\ y2，表示新勘探到了一块矩形石油区域，两个顶点坐标分别为(x1,y1)(x1, y1) 和 (x2,y2)(x2, y2)。坐标均为非负整数，yy 表示离地面的深度，输入保证 x1≤x2,y1≤y2x1 \leq x2, y1 \leq y2。
2. QQ -1，输出一个数量 mm, mm 表示当前最少需要多少口石油井才能开采出所有能开采的石油区域。
3. Q tQ\ t，输出勘探到的第 t+1t+1 块石油区域是否能被开采，能则输出 YesYes，否则输出 NoNo。输入保证 tt 大于等于零且小于当前已勘探到的区域数量。

### **边界情况说明**

1. 坐标含义：每一个整数表示一个格点，例如下图中的区域A，其坐标表示为 0 0 2 10\ 0\ 2\ 1，可以在 x=0,1,2x=0,1,2 三个位置打井挖出区域A的石油。
2. 可开采区域：最小深度小于等于 hh 的区域可以被开采，如下图中的区域D所示。
3. 区域连通：只有重叠的区域是连通的，边界紧邻的区域不是连通的，例如下图中的区域A,B,C是互相不连通的。

![](../附件/5ec2177e5337a993fca2764d9ddae3c9306cba96.png)

### **输出**

按照上述查询要求输出。每次查询输出后需换行。

### **输入样例**

```
5 40
A 39 22 46 25
Q -1
A 19 17 20 27
Q 1
A 35 6 45 7
Q 1
A 6 16 14 19
Q -1
A 11 16 12 26
Q 1
A 30 10 32 15
Q 0
A 18 15 27 17
Q 5
A 5 3 8 9
Q -1
A 3 24 8 25
Q -1
A 36 11 37 18
Q 6
A 47 16 56 24
Q -1
A 43 7 48 17
Q -1
A 44 11 46 13
Q 10
A 44 22 45 27
Q -1
A 45 7 48 12
Q 11
A 14 15 24 16
Q 8
A 43 20 44 30
Q -1
A 26 1 34 3
Q -1
A 13 5 19 11
Q -1
A 8 0 16 2
Q 15
```

### **输出样例**

```
0
No
No
0
No
No
No
1
1
No
1
1
No
1
No
No
1
2
3
No
```

### **限制**

0 <= h, x, y, N <= 12,000

时间限制：1 sec

内存限制：256 MB

**本题允许使用STL**

### **提示**

● 使用合适的数据结构高效地维护各区域之间的连通性，例如并查集。

● 使用合适的方法维护各区域是否被遮挡。

---

# CST 4-1-1 Split String

---

#### **描述**

字符串匹配问题，可以细分为两种类型：可重叠匹配，以及无重叠匹配。以文本串T="ababa"和模式串P="aba"为例，如果可重叠，便有两处匹配（T中作为前缀、后缀的“aba”）；反之若无重叠，则只有一处匹配（前缀、后缀“aba”只能取其中之一）。 若将任意串P在串T中无重叠匹配的数目记作g(T,P)g(T,P)，则不难验证：串T无论如何**拆分**为非空的前缀T1与后缀T2，都必有： g(T1,P)+g(T2,P)≤g(T,P) g(T1, P) + g(T2, P) \leq g(T, P)  我们在这里关注的是，有多少种拆分能使上式取等号。

#### **输入**

总共两行，分别给出文本串T、模式串P。

#### **输出**

只有一个整数，即无重叠匹配总数保持不变的**拆分**方式的总数。

#### **输入样例1**

```
ababa
aba
```

#### **输出样例1**

```
4
```

#### **输入样例2**

```
ababa
bab
```

#### **输出样例2**

```
2
```

#### **限制**

1≤|P|≤|T|≤1,000,0001 \leq |P| \leq |T| \leq 1,000,000

字符串均由小写英文字母构成

时间限制：1 sec

空间限制：256 MB

---

# CST 4-1-2 Pattern Matching

---

### **题目描述**

给定一个 MM 行 NN 列的二维字符矩阵 TT 和 kk 个 mm 行 nn 列的目标矩阵 Pi (1≤i≤k)P_i\ (1 \le i \le k)，求在 TT 中每个 PiP_i 出现的次数 tit_i：

### **输入格式**

输入第一行为五个整数 M,N,m,n,kM, N, m, n, k。

接下来的 MM 行依次给出矩阵 TT 的各行元素，每行有一个长为 NN 的字符串，表示 TT 的各个元素。

接下来 kk 部分，每部分有 mm 行。其中第 ii 部分的 mm 行依次给出目标矩阵 PiP_i 的各行元素，每行有一个长为 nn 的字符串，表示 PiP_i 的各个元素。

### **输出格式**

输出共 kk 行，在第 ii 行有一个整数 tit_i，表示所求的出现次数。

### **样例1输入**

```
4 4 2 2 2
abab
efcd
abcd
efcd
ab
ef
cd
cd
```

### **样例1输出**

```
2
2
```

### **约定与限制**

对前 30% 的数据，满足 1≤M≤50,1≤N≤50,1≤m≤2,1≤n≤2,1≤k≤11 \le M \le 50, 1 \le N \le 50, 1 \le m \le 2, 1 \le n \le 2, 1 \le k \le 1。

对前 50% 的数据，满足 1≤M≤200,1≤N≤200,1≤m≤6,1≤n≤6,1≤k≤101 \le M \le 200, 1 \le N \le 200, 1 \le m \le 6, 1 \le n \le 6, 1 \le k \le 10。

对前 80% 的数据，满足 1≤M≤2000,1≤N≤1000,1≤m≤10,1≤n≤10,1≤k≤1001 \le M \le 2000, 1 \le N \le 1000, 1 \le m \le 10, 1 \le n \le 10, 1 \le k \le 100。

对前 100% 的数据，满足 1≤M≤2000,1≤N≤1000,1≤m≤100,1≤n≤100,1≤k≤5001 \le M \le 2000, 1 \le N \le 1000, 1 \le m \le 100, 1 \le n \le 100, 1 \le k \le 500。

所有矩阵元素均为大小写字母。

- 时间限制：1.5s
- 空间限制：256MB

**本题允许使用STL**

### **提示**

- 串的哈希
- 虽然哈希比对不是100%准确，但只要将哈希冲突概率降至足够低，便能有极大把握通过本题。

---

# CST 4-1-3 Melody

---

### **题目描述**

你是一名来自魔法世界的作曲家。这天你创作了一段由 nn 个音符顺序连接而成的乐段 aa，其中每个音符都被表示成一个非负整数。因此，整个乐段可视作一个非负整数序列 {a1,a2,...,an}\{a_1,a_2,...,a_n\}。

你决定发行由该乐段生成的若干个版本，并收录进同一个专辑 AA。由于前奏在渲染气氛方面的不可替代性，你决定发行的每个版本都只能截取原乐段 aa 的一个前缀。也就是说，不同的版本仅在乐曲的长度，或者说在何处结束上有所差别。任何由乐段 aa 生成的乐曲都可以表示成序列 {a1,a2,...,ap}\{a_1, a_2, ..., a_p\}，其中 pp 是该乐曲的长度，满足 1≤p≤n1\le p \le n。

在你的世界里，评判专辑的好坏有一个专门的度量，叫做和谐度。它的定义为，其中收录的所有乐曲最长的共有的前奏和尾声。具体而言，对于一个由 kk 首曲子构成的专辑 A={a1,a2,...,ak}A = \{a^1, a^2, ..., a^k\} ，一个序列 b={b1,b2,..,bl}b=\{b_1, b_2, .., b_l\} 为该专辑共有的前奏和尾声的充要条件为，对于任意的 1≤i≤k1\le i\le k，存在长度非负的序列 xix_i，使 aia^i 能表示为 b⋅xi⋅bb \cdot x_i \cdot b 的形式，其中 ⋅\cdot 为字符串拼接运算符。在所有满足条件的 bb 当中，具有最大序列长度的解，令其为 bopt={bopt1,bopt2,...,boptlopt}b^{opt} = \{b_1^{opt}, b_2^{opt}, ..., b_{l^{opt}}^{opt}\}，那么该专辑的和谐度就定义为 loptl^{opt}。

现在，你需要支持 mm 个操作，每个操作都属于以下两种类型之一：

- 创作：制作专辑的过程使你迸发了新的灵感，你在原本的乐段末尾又增添了 qq 个音符。
- 测试专辑：你想了一个发行专辑的方案。在这个方案中，专辑由 qq 首曲子构成，每首曲子的长度给出，你需要计算出根据定义，该方案中专辑的和谐度。

只有你正确地实现所有的 mm 个操作，才能成功地发行专辑，加油吧魔法作曲家！

### **输入输出格式描述**

**输入格式**

第一行包含两个正整数 nn, mm，分别表示初始乐段的长度和操作数。

第二行包含序列 aa，由 nn 个非负整数构成，其中第 ii 个数为 aia_i。

接下来的 mm 行，每行描述一种操作，分别遵循如下格式：

- `1 q c1 c2 ... cq` 创作类别操作，由数字 1 开头，接下来是正整数 qq，表示这次创作过程中增添的音符数量。接下来是 qq 个非负整数，依次描述了添加到之前乐段末尾的音符。注意，音符按照添加进乐段的顺序给出，并且该操作完成后效果保留，会继续影响之后的操作。
- `2 q d1 d2 ... dq` 测试专辑类别操作，由数字 2 开头，接下来正整数 qq，表示这个专辑的方案当中共有 qq 首曲子。接下来是 qq 个正整数，分别描述了专辑当中的 qq 首曲子。其中第 ii 个数为 did_i，表示专辑中的第 ii 首曲子是当前乐段的长度为 did_i 的前缀。注意，询问中的 did_i 的大小可能超过初始乐段长度 nn ，但保证不会超过当前乐段的总长度，即通过创作操作增添新的片段达到的目前的长度。

**输出格式**

对于每一个测试专辑类别的操作，输出一行表示答案，即操作描述中给定专辑的和谐度。

### **样例输入输出**

**输入样例 1**

```
4 2
1 2 1 2
1 1 1
2 1 5
```

**输出样例 1**

```
1
```

**输入样例 2**

```
5 4
1 2 1 2 1
1 3 2 1 2
2 1 8
2 2 6 8
2 2 7 8
```

**输出样例 2**

```
4
2
0
```

### **数据规模**

对于 50%50\% 的数据，保证所有操作中 n,m≤5000n,m\le 5000。

另有 30%30\% 的数据，保证测试专辑类别操作中的参数 qi=1q_i = 1。

对于 100%100\% 的数据，1≤n,m≤5×105，0≤ai,ci,di≤109，∑iqi≤1061\le n,m\le 5\times 10^5，0\le a_i,c_i, d_i\le 10^9，\sum_{i} q_i \le 10^6 （所有1、2类操作的第二个参数 qq 的加和不超过 10610^6）。

时间限制：1s

空间限制：256MB

**本题允许使用STL**

---

# CST 4-2-1 Process Schedule

---

### **描述**

有一个运行在单核CPU上的简单操作系统，它任一时刻最多只能执行一个进程，我们要为它设计一个简单的进程调度器。

进程：

1. 这个操作系统上的每个进程在创建时会被赋予一个**进程号**，不同进程的进程号均不相同；
2. 每个进程还存在一个**优先级**，优先级高的进程会抢占当前正在运行的、优先级较低的进程（抢占指使CPU暂停执行优先级低的进程，转而执行优先级高的进程）；
3. 此外，进程还有一个**创建时间**和执行完毕所需的**执行时间**；
4. 进程执行完毕后就不再占用CPU资源，也不会再被进程调度器调度了。

进程调度器的任务是，面对源源不断到来的进程流，选择一个进程占据CPU执行，具体而言：

1. 在任一时刻，进程调度器总是选取该时刻及其之前所有没有执行完毕的进程中优先级最高的进程占据CPU执行
2. 当新来一个待执行进程时，如果该进程的优先级大于正在执行的进程的优先级，则发生抢占，正在执行的进程让出CPU给高优先级进程执行
3. 如果两个进程的优先级相同，则采取先到先得的方案，创建时间小的先执行；如果创建时间相同，则进程号小的先执行

### **输入**

共计N + 1行，第一行表示有多少个进程需要被调度。

接下来N行，每行代表一个需要被调度的进程，每个进程以

```
进程号 优先级 创建时间 执行时间
```

的方式给出。

我们保证，这N个进程的创建时间非减，以模拟真实的进程流。

### **输出**

共计N行，每行输出第i个执行完毕的进程的进程号及其完成时间。

### **输入样例**

```
10
1 1 2 2
4 1 2 2
3 1 4 1
2 1 4 1
5 1 5 1
6 2 5 1
7 1 7 10
8 2 8 2
9 3 9 3
10 4 9 5
```

### **输出样例**

```
1 4
6 6
4 7
2 8
10 14
9 17
8 18
3 19
5 20
7 30
```

### **限制**

进程号、优先级、创建时间、执行时间、结束时间在int范围内。

1 ≤ N ≤ 10<sup>6</sup>

时间：1 sec

空间：256 MB

### **提示**

- 优先级队列

---

# CST X-3-1 Component

---

### **描述**

给一个有 n n  个点 m m  条边的带点权的无向图 G G  和一个整数 k k ，你需要支持下面两种操作：

- 加边：在 G G  中加入一条新边 (u,v) (u, v) ；
- 查询：求 u u  所在连通分量中第 k k  大的点的权值，如果 u u  所在的连通块中点的数量不足 k k ，则查询结果为 −1 -1 。

*注：原有的图和加过边的图中都可能有自环和重边。*

### **输入格式**

第一行四个整数 nn，mm，kk 和 qq。

接下来 1 行是 nn 个点的权值 a1,a2,⋯,ana_1, a_2, \cdots, a_n，权值均为整数，保证 0≤ai<1090 \le a_i < 10^9。

接下来 mm 行，每行两个整数 uu 和 vv，保证 1≤u,v≤n1 \le u, v \le n，表示 uu 和 vv 之间有一条边。

接下来 qq 行，每行表示一个操作，第一个整数是 opop 用于标示操作类型，opop 只有 11 和 22 两种取值。

- 如果 op=1op = 1，则说明这是一个加边操作，接下来有两个整数 uu 和 vv，保证 1≤u,v≤n1 \le u, v \le n；
- 如果 op=2op = 2，则说明这是一个查询，接下来有一个整数 uu，保证 1≤u≤n1 \le u \le n。

### **输出格式**

对于每一个第二种操作，输出一行一个整数 kk。

### **输入样例**

```
4 2 2 9
11 12 13 14
1 1
1 2
2 1
2 2
2 3
1 3 4
2 3
1 3 4
2 4
1 2 3
2 1
```

*此样例是第 1 个测试点。

### **输出样例**

```
11
11
-1
13
13
13
```

### **数据范围**

1≤n,k,q≤1061 \le n, k, q \le 10^6

0≤m≤1060 \le m \le 10^6

### **资源限制**

时间限制：3 sec

空间限制：256 MB

栈空间大小：256 MB

**本题允许使用STL**

### **提示**

本题有多种解法，可能涉及的知识点包括

- 并查集
- 左式堆

---

# CST 4-2-2 Sort

---

### **描述**

一种新的比较器硬件被发明了出来，它可以每次比较三个数，并返回最小数和最大数。

为了测试这个比较器，你需要实现一个排序算法，使用该比较器，来将 n 个**互不相同**的整数从小到大排序。

具体来说，我们提供了一个交互库，其中实现了一个比较器函数，你需要编写一个排序函数，调用比较器函数将我们传给你的序列排好序即可。

由于测试时间紧迫，比较器不能无限次使用，你需要在比较器使用次数限制之内，将这些整数正确地排序。

### **接口说明**

我们提供的交互库包括一个头文件 sort.h 和一个库文件 sort_lib.cpp。头文件包含了排序函数和比较器函数的声明，库文件包含了比较器函数和主函数的实现。编译时，我们会将库文件和你的程序一起编译和链接，因此你只需在你的程序中包含头文件 sort.h，并实现排序函数。

以下是各函数的声明：

```
void sort(int n, int limit, int *a);
```

这是排序函数的声明。你需要在你的代码中实现该函数。其中 n 表示待排序的整数个数，limit 表示比较器函数的使用次数限制。a 为一个长度为 n 的整数数组，初始值为 0 ~ n-1。你需要将排序的结果存入 a 数组中。

```
void compare(int a, int b, int c, int *max, int *min);
```

这是比较器函数的声明。我们的交互库中已经实现了该函数，你需要在排序时调用它。传入的 a, b, c 表示待比较的三个数，比较完成后，最大的数会被存入 max 指针所指的地址中，最小的数会被存入 min 指针所指的地址中。

为了防止作弊，你无法得到待排序的整数的值，而只能得到每个整数的**编号**。因此 a 数组中的值，以及传给比较器函数的 a, b, c，都应该是 0 ~ n-1 之间的数，表示某个整数的编号。

### **测试说明**

我们的交互库的具体实现如下：

首先从标准输入中读取两个整数 n, limit，分别表示待排序的整数个数和比较器函数的使用次数限制。

然后以某种方式读入并生成待排序的 n 个整数，这一步对于我们提供的交互库和OJ上评测时使用的交互库可能不同（但这不影响你做题）。

接着调用 sort 函数。

在你实现的 sort 函数中，可以调用 compare 函数来进行比较，如果传入的 a, b, c 不在 0 ~ n-1 的范围内，或 compare 函数调用次数超过 limit，你的程序会被判为“Wrong Answer”。

在 sort 函数返回后，我们会检查 a 数组的内容，是否已经正确地排好序，如果不是，你的程序会被判为“Wrong Answer”，否则你的程序被判为“Accepted”，即通过该测试点。

如果你需要在本地测试你的程序，你可以使用 g++ sort_lib.cpp your_code.cpp -std=c++14 -O2 -o sort.exe 编译你的程序，再运行 sort.exe 来测试。

你需要严格按照输入格式向交互库输入数据，否则不能保证交互库正常运行。

在交互库运行结束后，会输出排序正确与否等信息，到标准输出和标准错误输出，你可以参考 sort_lib.cpp 来了解更多。注意，当你的程序异常退出时（如栈溢出），你可能无法得到这些信息的输出。

我们还提供了一个样例程序 sort_sample.cpp，实现了 Bogo 排序算法。你可以参考这个程序来理解交互库和解题，或者不参考，这将与你的得分无关。

请注意，你只需提交实现了 sort 函数的源文件，不要提交交互库“sort.h”“sort_lib.cpp”等其他文件，否则可能会导致编译错误。

[下载交互库和样例程序](../附件/8bd4daee1f829efa7d14468c05bf7a762c955c62.zip)

### **输入**

对于我们提供的交互库，输入格式如下：

第一行两个整数 n, limit，具体含义见“测试说明”。

第二行 n 个整数，要求是一个 0 ~ n-1 的排列，表示待排序的序列。

### **输出**

对于我们提供的交互库，当你的程序能够在比较次数限制之内正确地进行排序时，输出“Accepted”，否则会输出一些错误信息，具体见交互库源程序 sort_lib.cpp。

### **输入样例**

```
5 1000
3 2 4 0 1
```

这是对于我们提供的交互库的一组输入样例，可用于本地调试。在 OJ 上评测时，由于交互库实现不同，输入格式不一定是这样。

**请不要尝试在你的程序中读取标准输入或其他文件。**

### **输出样例**

```
Accepted
```

标准错误输出(stderr)中的输出：

```
cmp_cnt = 135
```

这是使用 sort_sample.cpp 和我们提供的交互库输入样例的运行结果，表示该排序算法能通过这组数据，调用了比较函数 135 次。

**请不要尝试在你的程序中向标准输出或其他文件中输出信息。**

### **数据范围**

共有20个测试点，每个测试点的数据范围如下：

<table class="table-bordered table-striped table"><tbody><tr>
<th> 测试点编号 </th><th> n </th><th> limit </th>
</tr>

<tr>
<td> 1 </td><td> 10 </td><td> 1000 </td>
</tr>

<tr>
<td> 2 </td><td> 100 </td><td> 10000 </td>
</tr>

<tr>
<td> 3 </td><td> 1000 </td><td> 300000 </td>
</tr>

<tr>
<td> 4 </td><td> 10000 </td><td> 200000 </td>
</tr>

<tr>
<td> 5 </td><td> 10000 </td><td> 100000 </td>
</tr>

<tr>
<td> 6 </td><td> 10000 </td><td> 90000 </td>
</tr>

<tr>
<td> 7 </td><td> 10000 </td><td> 80000 </td>
</tr>

<tr>
<td> 8 </td><td> 10000 </td><td> 74000 </td>
</tr>

<tr>
<td> 9 </td><td> 10000 </td><td> 72000 </td>
</tr>

<tr>
<td> 10 </td><td> 1000000 </td><td> 13000000 </td>
</tr>

<tr>
<td> 11 </td><td> 100000 </td><td> 3500000 </td>
</tr>

<tr>
<td> 12 </td><td> 100000 </td><td> 2000000 </td>
</tr>

<tr>
<td> 13 </td><td> 100000 </td><td> 1000000 </td>
</tr>

<tr>
<td> 14 </td><td> 100000 </td><td> 930000 </td>
</tr>

<tr>
<td> 15 </td><td> 100000 </td><td> 900000 </td>
</tr>

<tr>
<td> 16 </td><td> 100000 </td><td> 870000 </td>
</tr>

<tr>
<td> 17 </td><td> 1000000 </td><td> 11700000 </td>
</tr>

<tr>
<td> 18 </td><td> 1000000 </td><td> 11000000 </td>
</tr>

<tr>
<td> 19 </td><td> 10000 </td><td> 70000 </td>
</tr>

<tr>
<td> 20 </td><td> 1000000 </td><td> 10300000 </td>
</tr>
</tbody></table>

### **资源限制**

时间限制：1 sec

内存限制：256 MB

**本题允许使用STL**

### **提示**

● [有哪些常用的排序算法？这些算法的期望、最好、最坏比较次数分别是多少？]

● [给 n 个数排序的比较次数的理论下界是多少？如果一次能比较三个数，又是多少？]

● [哪些排序算法可以改进成一次比较三个数的？改进效果分别如何？]

● [提示：对已有的排序算法作更多的改进，以进一步减少比较次数]
